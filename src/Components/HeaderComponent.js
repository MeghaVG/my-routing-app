import React from "react";

const HeaderComponent = () => {
  return (
    <div
      style={{
        color: "red",
        padding: "10px",
        width: "100%",
        border: "1px solid red",
      }}
    >
      Component Header
    </div>
  );
};

export default HeaderComponent;
